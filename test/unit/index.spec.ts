import { expect } from "@infra-blocks/test";

describe("index", () => {
  describe("dummy test", () => {
    it("should return the right stuff", () => {
      expect(true).to.be.true;
    });
  });
});
